# build output
dist/
.astro/

# deps
node_modules/

# env / secrets — Joshua authorizes each login himself; never commit credentials
.env
.env.*
!.env.example

# os / editor
.DS_Store
Thumbs.db
*.log
npm-debug.log*

# large source archives & raw photo dumps live one level up, not in the repo
*.zip
